# Crew upgrade execution record

This is an additive upgrade of the existing Million Beer Project. Production data preservation takes precedence over release completion. Private exports, account identifiers, row manifests, and live screenshots belong outside this repository.

## Baseline — arrival checkpoint, 2026-09-09

- Source and remote main at arrival: `ab746e2aabec8774cf5f025ab466c68bf31377d8`; clean working tree on arrival.
- Work branch: `codex/million-beers-crew-upgrade`.
- Unique recovery tag: `rollback/million-beers-pre-crew-20260909-2334` (never move it).
- Last main CI at arrival: [30774177769](https://github.com/Arhaan2/million-beers-by-2035/actions/runs/30774177769), successful.
- Existing Pages URL: <https://arhaan2.github.io/million-beers-by-2035/>.
- Read-only API baseline: canonical total 1,107; 317 parent entries; 331 allocations. These are a live response, **not** a verified consistent database snapshot.
- Challenge configuration remains target 1,000,000; start `2026-07-24T00:00:00-07:00`; deadline `2035-01-01T00:00:00-08:00`; timezone `America/Los_Angeles`.
- Before desktop/mobile browser screenshots and API responses captured privately outside Git. Frozen baseline source retained for independent quality checks while local implementation proceeds. Formatting, lint, typecheck, Worker dry-run build, and frontend build passed. All 36 frontend tests passed; 41/42 API tests passed on the first run, with the pre-existing signature-tamper test intermittently substituting a base64 character without changing signature bytes. The test now mutates an actual signature byte; authentication behavior is unchanged.
- GitHub owner permissions verified at arrival; no open PRs then; branch protection API reported no protection. No protections changed.
- Original Pages artifact expired. Deployed index and six static assets preserved privately with SHA-256 checksums; frozen baseline builds also pass.

## Phase status and gates

Current checkpoint: the corrected migrations, compatible Worker and Pages frontend are live, new capabilities are enabled, and PR #3 is merged. The deployed release commit and all nine artifact file hashes match. Final immutable-ID/projection reconciliation and all six read-only live browser cases passed. Release phases, final capability presentation review and verification-process cleanup are complete. Earlier blocked checkpoints below are retained as dated history.

| Phase                        | Status                        | Evidence / remaining gate                                                                                                                                                                                                          |
| ---------------------------- | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0 Baseline and recovery      | PASS                          | Protected full export, verified private SQLite and Worker/D1 restorations, immutable-ID manifests, actual migration/configuration inventory, preserved original artifacts, and verified 30-day Time Travel window.                 |
| 1 Reliability                | COMPLETE                      | Revision ordering, exact durable retries, shared draft locking and response-loss recovery passed 77 frontend tests and six browser cases. Six read-only live browser cases also passed.                                            |
| 2 Crew directory             | COMPLETE                      | 36 member projections and 329 mapped allocations reconcile; two Anonymous allocations remain unmapped. No historical system classification or inferred identity changes.                                                           |
| 3 Occurrence and corrections | COMPLETE                      | Optional occurrence dates, DST validation and atomic append-only linked correction limits passed transaction and concurrency tests.                                                                                                |
| 4 Memories and history       | COMPLETE                      | Text memories, private-field enforcement, bounded history, member views and recaps enabled. Photos remain disabled and hidden.                                                                                                     |
| 5 Dashboard/mobile           | COMPLETE                      | Preserved visual identity and six built-app browser/viewport cases pass. Release commit and assets match; live browser cases passed. Capability presentation reviewed and verification processes exited.                           |
| 6 Sharing/privacy/about      | COMPLETE                      | Saved-data export, explicit sharing, generic crawler preview and About metrics pass local checks. Stronger identity, dynamic event previews and editor invitations remain unavailable.                                             |
| 7 Rehearsal                  | PASS                          | Private real-data restoration preserved every original ID/hash; old deployed Worker/new schema, legacy inserts, metadata, concurrent corrections, privacy and capability-off recovery passed.                                      |
| 8 Push/deploy                | COMPLETE — backend/Pages LIVE | Runtime candidate and CI passed. Migrations applied individually, Worker deployed and guarded capability enablement verified. Pages artifact matches; live browser checks and final immutable-ID/projection reconciliation passed. |

## Non-negotiable release checks

1. Do not edit applied migrations, modify immutable ledger fields, reset a database, run synthetic production writes, or restore an old snapshot onto production.
2. Verify actual remote migration records before assigning/applying the candidate additive migration. Local numbering is provisional until that check passes.
3. Take a fresh protected export; restore it privately; reconcile parent/allocation/state/aggregates; capture immutable hashes per original ID. Compare IDs, not timestamps or a frozen expected total, after real intervening writes.
4. Prove old frontend/new API and old-code inserts/expanded schema. After privacy controls exist, the original Worker is not a safe rollback: it cannot honor projection privacy. Use the tested compatible API with new mutation capabilities disabled.
5. Deploy only an exact committed candidate after CI, full transaction-path tests, recovery rehearsal, and independent safety review. Backend readiness precedes merge and Pages. Preserve the repository base path.
6. Optional unavailable features must be hidden and reported honestly. No R2 activation, billing changes, stronger identity claims, inferred personal contact details, or pretend uploads.

## Checkpoints

The four checkpoints immediately below record the earlier state before Cloudflare access resumed; their blockers and source tags are historical, not current release status.

Each coherent phase is committed on the feature branch. Final release evidence records test commands/results, browser engine limitations, compatibility, enabled capabilities, commit/PR/CI/release links, and unresolved gates. Documentation does not substitute for database evidence.

### Backend/data checkpoint — 2026-09-09, before access resumed

- Candidate additive migrations 0003 (crew/revision/metadata/corrections) and 0004 (atomic owner audit operations) pass isolated tests; numbering remains subject to remote verification. Applied source migrations 0001/0002 are unchanged.
- Actual Workers/D1 runtime: 74/74 API tests passed, including concurrency, legacy bridges, failed migration rollback/resumption, pagination, privacy, and capability-off rollback after new writes.
- Synthetic Python integrity/operator suite: 17/17 passed. No production SQL export exists: backup/restore and checkpoint preservation of live IDs remain blocked by Cloudflare authorization.
- No historical operational entries classified: exact production-ID evidence has not been verified. New community counts currently exclude only explicitly classified records.

### Frontend/reliability checkpoint — 2026-09-09, before access resumed

- Formatting, lint, and typecheck passed for the complete candidate. Frontend: 77/77 tests; API: 74/74 actual Worker/D1 tests; Python: 17/17. Both builds passed.
- Independent review found and resolved a cross-tab autosave/discard race; every shared-draft mutation now joins the submission lock and confirmed completion clears only its exact key. No new submission is dispatched without durable shared storage and a safe lock. Exact unresolved retries retain their original payload.
- Desktop/mobile dashboard rendered in Chromium, Firefox, and WebKit. Chromium/Firefox console clean; WebKit console clean before screenshots, with a reproduced screenshot-tool stylesheet CSP message after capture only. Production CSP was not weakened.
- Preserved original frontend renders the synthetic new API and successfully logs a synthetic +1 through its original shared-code form; totals and parent count increase once.
- Shareable before/after screenshots in `docs/qa` contain synthetic local data only. Live before evidence remains outside Git. Full scripted browser write/retry/route checks subsequently passed in all six engine/viewport cases.

### Release verification checkpoint — 2026-09-09, before access resumed

- `npm run test:browser`: six complete cases passed, using Chromium, Firefox and WebKit at 1440×1000 desktop and 390×844 touch emulation against a fresh actual local Worker/D1. Every case exercises login, single/group recording, linked partial correction, commit-response loss, modal closure/reload/reauthentication and retry of the original exact key. Directory beyond the leaderboard, hash routes/reload/back-forward, malformed/missing records, keyboard focus and horizontal overflow checks passed. No unexpected application or network errors. See the sanitized [synthetic browser report](qa/browser-report.json).
- WebKit's screenshot helper injects a stylesheet and produces a known CSP diagnostic only during screenshot capture; that diagnostic is separately counted. Planned response-abort diagnostics are also separately counted. Application CSP/CORS checks remain enforced.
- `npm run test:release`: 20 actual-script local HTTP tests pass, including malformed/missing readiness values and schema, unsupported schema, incorrect source tag, unavailable capabilities, bad HTTP status and non-JSON responses. Readiness requires strict boolean values and supported integer schema 3.
- Independent review resolved both final release-control findings: strict readiness validation and a manual main-only executable original-frontend rollback workflow retaining the compatible API. No unresolved core integrity/privacy finding was reported by the independent reviewer.
- Hosted preview is unavailable because Cloudflare Worker/D1 access is rejected. Equivalent built-artifact testing ran locally, with synthetic fixtures, noindex headers, isolated credentials/configuration and separate temporary state. No physical device testing was performed.
- Existing dependency audit reports nine pre-existing development-tool dependency advisories (four moderate, five high); no blind dependency upgrades were made. The added browser runner is pinned to Playwright 1.63.0.
- Rechecked remote main: still `ab746e2aabec8774cf5f025ab466c68bf31377d8`. Rechecked D1 migration access: still authentication-rejected. The final read-only public response remains 1,107 total, 317 parent entries and 331 allocations, matching the initial public observation. No production entry was created, classified, corrected, or modified by this work. Matching public totals cannot prove per-ID preservation or replace a consistent database snapshot; that production gate remains open.

- Final same-data before/after screenshots were inspected at 1440×1050 and 390×844. Saved occasion/recap card exports produced valid 1200×630 PNGs; copy-link routes matched and no automatic share or mutation occurred. All local browser/Worker servers were stopped.

### Published checkpoint — 2026-09-09, before access resumed

- Feature branch and immutable rollback tag were pushed; [draft PR #3](https://github.com/Arhaan2/million-beers-by-2035/pull/3) contains migration/rollback notes and synthetic before/after screenshots. The implementation and release-controls checkpoint is `7bd7f69b76f998961aaed80f3a14ba523b396793`; subsequent execution-record-only commits do not change the candidate Worker source tag.
- [PR CI checks](https://github.com/Arhaan2/million-beers-by-2035/pull/3/checks) run the complete quality/build and isolated browser suites. PRs cannot upload or deploy Pages artifacts.
- Backend candidate source tag: `crew-39f0cf60dc6de66cba025f19369b53323c589476`. No production Worker release ID is claimed: current identity could not be read with the available Cloudflare authorization. No remote migrations were applied by this work; actual remote migration state is still unverified.
- Main and the existing production URLs remain unchanged. Do not merge or deploy this PR until the private production recovery, migration and per-ID integrity gates above have passed.

### Access and recovery resumed — 2026-09-09

The earlier checkpoint blockers were resolved during this access-and-recovery phase. Resource-specific access now succeeds with a directory-bound Wrangler OAuth profile granting account/user read, Worker-script write and D1 write. No environment token overrides were present. Shared login settings and secrets were preserved.

A complete private production export finished in 2.785 seconds after its possible query-blocking impact was announced. SHA-256: `33d6e6594b7a871d76f406e00cfb200fddc5ba94d9cea6f346dad917074908d3`. Independent SQLite and isolated Worker/D1 restorations verified 317 entries, 331 allocations and total 1,107, every original immutable ID/hash, relationships, ordering and contributor/day aggregates. A current Time Travel bookmark is retained privately; successful 29-day lookup and rejection beyond 30 days confirmed the actual 30-day window. No Time Travel restore was run.

Real-data rehearsal passed old deployed Worker writes on 0003 alone and both migrations, old event-only inserts, exact retries, metadata edits, concurrent corrections, privacy redaction and the reviewed capability-off fallback. No preliminary Worker bridge is required. Subsequent private single-statement snapshots preserve original IDs while allowing legitimate appends; they supplement the complete export and separate schema checks.

The first remote 0003 attempt was rejected with `incomplete input`. Readback proved the original schema and only migrations 0001/0002 remained, with all original ledger hashes unchanged. Availability samples returned HTTP 200. The trigger guards matched the documented [D1 CASE/END parser issue](https://github.com/cloudflare/workers-sdk/issues/4727). Before either migration was applied, the pending 0003/0004 files were corrected by parenthesizing only their ten CASE guards; no applied migration or constraint behavior was changed. Local tests use Wrangler's client-side SQL splitter, while remote migration requests send raw SQL, so passing local execution alone did not expose this parser difference.

At the end of that recovery checkpoint, release progression paused for verification of the targeted syntax fix. No Worker deployment, capability enablement, merge or Pages deployment had occurred yet. The original frontend, Worker artifact, complete export and private manifests remain preserved.

### Verified release and final reconciliation — 2026-09-10 UTC

- Tested runtime candidate: `a6ce7ee60ae3703401db79abf07dc6979e19dceb`. Formatting, lint, typecheck, Worker dry-run and frontend production builds passed on that clean exact commit. Frontend 77/77, actual Worker/D1 74/74, Python integrity/operator 17/17, release-gate 20/20, and six complete Chromium/Firefox/WebKit desktop/mobile emulation cases passed. No physical-device testing was performed. [Candidate CI 34424458417](https://github.com/Arhaan2/million-beers-by-2035/actions/runs/34424458417) passed.
- Corrected additive migrations 0003 and 0004 were applied separately, with a consistent snapshot and independent projection/schema checks after each step. Actual executable schema matches the corrected source after allowing for remote comment stripping. Applied 0001/0002 remained unchanged. Every original immutable entry/allocation hash, relationship, ordering, canonical total and contributor/day aggregate remained intact: 317 entries, 331 allocations, total 1,107. No intervening ledger appends occurred during these checkpoints. Crew projections reconcile to 36 members and 329 mapped allocations; two Anonymous allocations remain unmapped.
- Worker version `1e873434-b7da-4c20-96d4-785f511ec773` deployed with exact backend content tag `crew-571550e9422d44df680672557ad708766a2cabe1`. Existing bindings, variables, secret names and runtime settings matched the prior deployment; `VERSION_METADATA` was the only added binding. Existing secrets were preserved.
- Readiness and legacy public API contracts passed with new mutations disabled. One guarded owner capability audit then changed enabled state from 0 to 1 and revision from 317 to 318, with no ledger delta. Crew, history, memories, occurrence dates, linked corrections, metadata editing, member creation and enhanced logging are enabled. Photos and stronger identity remain disabled; no storage activation or identity claims were added.
- [PR #3](https://github.com/Arhaan2/million-beers-by-2035/pull/3) merged as `9565d943abe883cdb073db481e81d7b5314eaa1b` at `2026-09-10T01:21:56Z`. The merge tree exactly matches tested candidate `a6ce7ee60ae3703401db79abf07dc6979e19dceb`.
- Availability monitoring recorded 180 periodic Node-fetch API samples, all HTTP 200 with zero sampled errors, from `2026-09-10T01:05:36.093Z` through `2026-09-10T01:21:11.888Z`. These samples cover the rejected and corrected migrations, Worker deployment and capability enablement; they do not prove uninterrupted availability between samples. The announced export command took 2.785 seconds; exact query-blocking duration during export was not measured. A separate Python urllib health probe immediately after export returned HTTP 403, while an immediate equivalent Node-fetch probe returned HTTP 200. That client-specific observation is recorded separately and does not establish a database outage.
- The deployed compatible Worker module, checksum and version metadata are preserved privately for recovery. No credentials were logged or retained in the release evidence. The nine previously reported development-tool advisories remain unchanged; no dependency upgrades were made. Physical devices and the hosted rollback workflow were not exercised.
- [Pages run 34425172732](https://github.com/Arhaan2/million-beers-by-2035/actions/runs/34425172732) completed successfully, including build and deployment. The [live site](https://arhaan2.github.io/million-beers-by-2035/) serves `release.json` with exact merge commit `9565d943abe883cdb073db481e81d7b5314eaa1b`; all nine deployed artifact files match their SHA-256 checksums. The repository base path is preserved.
- Read-only public contracts retain total 1,107, 317 entries, 331 allocations and 36 named contributors. The new directory contains 36 members and community statistics report 18 active participants with recorded allocations in the last 30 days. No historical system classification was applied. These public counts supplement the private per-ID checks; they do not replace them.
- Final post-release reconciliation passed at `2026-09-10T01:31:13Z`: all 317 original entries and 331 original allocations retain their immutable hashes, relationships and ordering; state and contributor/day aggregates remain total 1,107. There were zero intervening ledger appends. Projections reconcile to 36 members, 329 mapped allocations and two Anonymous allocations. Metadata, corrections and system classifications remain empty. Mutations are enabled at revision 318 with exactly one capability audit. These assertions were checked against both the original checkpoint and the release steps; no row manifests or source rows are published.
- Read-only live Chromium, Firefox and WebKit checks passed at both desktop and mobile emulation viewports, with zero application console, page or network errors. WebKit passed without screenshot capture; its first desktop capture run recorded five screenshot-helper CSP diagnostics for five screenshots, separately identified as capture-tool effects. Application CSP was unchanged. No live authentication attempts, mutation requests or blocked requests occurred. The final capability presentation review passed: About describes unavailable photos/invitations and shared-code/unverified-name limitations accurately, no photo uploader is exposed, and photos/stronger-identity flags remain disabled. All verification processes exited.
- Recovery materials were copied to persistent protected private storage with directory mode 700 and file mode 600; the full export checksum was reverified. The original artifacts and the deployed compatible Worker, metadata and checksums are retained. Raw account/database identifiers, row data, snapshots, SQL plans, manifests and live screenshots remain outside Git.
- Test limitations remain explicit: no physical-device testing, no hosted frontend rollback invocation, and no production write tests. Real mutation, retry, correction, privacy and compatibility tests used isolated local data. Nine pre-existing development-tool dependency advisories remain; no dependency updates were made during release verification. No production synthetic entries, corrections or classifications were created.
